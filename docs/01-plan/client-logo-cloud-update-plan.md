# 클라이언트 로고 클라우드 업데이트 계획 (Client Logo Cloud Update Plan)

> 작성일: 2026-03-13
> 담당자: AI Assistant (Antigravity)
> 목적: 기존 19개의 임시 클라이언트 로고(.webp)를 44개의 실제 병원 로고(.png)로 교체

---

## 1. 현재 상황 및 목표
* **현재**: `public/assets/images/client/` 폴더 내에 19개의 임시 파일(`client-01.webp` ~ `client-19.webp`)을 사용 중.
* **목표**: 사용자가 제공한 44개의 실제 클라이언트 로고 이미지(한글 이름의 PNG 파일들)를 적용하여 3D 클라우드의 내용을 보다 밀도 있고 실제적인 레퍼런스로 채움.

---

## 2. 해결 과제 및 사전 작업 (완료됨)
* **한글 파일명 이슈 방지**: 
  한글 확장자나 파일명은 Vercel 배포 과정 중 URL 인코딩 문제나 리눅스 파일 시스템(NAS 등)에서 종종 문제를 일으킬 수 있습니다.
* **사전 조치 완료**: 
  이를 미연에 방지하기 위해 스크립트를 통해 `거인병원.png` 등의 원본 한글 파일명을 `ref-001.png` ~ `ref-044.png` 라는 안전한 영문+숫자 형태로 이미 모두(44개) **일괄 변경 완료**했습니다. (원본 병원 이름은 `alt` 속성을 위해 데이터 자체에 보존합니다.)

---

## 3. 업데이트 실행 단계 (Action Items)

### 단계 1: 데이터 소스 업데이트 (`src/data/clients.ts`)
* 현재 19개 강제 생성(Array.from) 로직을 제거합니다.
* 새로 정리된 44개의 객체 배열 데이터(id, 병원 이름, `ref-XXX.png` 경로)로 `CLIENT_LOGOS` 상수를 완전히 갈아끼웁니다.
* *이를 통해 3D 캔버스 컴포넌트(`ClientIconCloud`)가 44개의 요소를 자동으로 렌더링하게 됩니다.*

### 단계 2: 기존 더미 이미지 삭제 (Clean Up)
* `public/assets/images/client/` 폴더 내에 있던 기존 `client-*.webp` 더미 파일 19개를 삭제하여 빌드 용량을 최적화합니다.

### 단계 3: 컴포넌트 및 UI 렌더링 확인
* 로컬 서버(localhost:3000) 화면에서 로고 44개가 구 형태(Sphere) 안에서 자연스럽게 돌아가는지 확인합니다.
* (예상) 요소가 44개로 늘어나면서 밀집도가 높아져 훨씬 화려하고 신뢰도 높은 모션이 보여질 것입니다.

---

## 4. 즉시 실행 요청
본 계획안에 동의하시면 **"진행해줘"** 라고 말씀해 주세요! 
수정 및 불필요한 이미지 삭제까지 깔끔하게 일괄 적용해 드리겠습니다.
